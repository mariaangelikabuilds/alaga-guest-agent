{
  "name": "alaga-guest-agent",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "description": "A guest-facing hospitality AI agent: long journey memory, real workflow triggers, and a human-escalation gate. Built on the Claude SDK.",
  "scripts": {
    "seed": "tsx src/seed.ts",
    "demo": "tsx demo/run.ts",
    "chat": "tsx src/cli.ts",
    "test": "tsx src/guardrails.test.ts"
  },
  "dependencies": {
    "@anthropic-ai/sdk": "^0.65.0"
  },
  "devDependencies": {
    "tsx": "^4.19.2",
    "@types/node": "^22.10.0"
  }
}
